# Sectio Project Todo

i need to fix database first
there is mess in namig of properties

## Database Fixes
- [x] Fix CHS family: `Iy` and `Iz` are zero in `steel_profiles.db`.
- [x] Verify `SHS` and `RHS` families for similar zero-value issues.
- [ ] Audit `DATA_DICTIONARY` for missing units or property definitions.

## Engine Enhancements
- [ ] Implement Plastic Section Modulus ($W_{pl,y}, W_{pl,z}$).
- [ ] Add Torsional Constant ($J$) for open sections (UPN/IPN).

